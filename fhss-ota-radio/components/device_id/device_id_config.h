#pragma once

/* MAC 주소 뒤 3바이트만 사용 — 앞 3바이트(OUI)는 같은 Espressif 칩을 쓰는
 * 모든 보드가 동일해서 구분에 의미가 없고, 실질적 고유성은 뒤 3바이트
 * (칩마다 공장에서 고유 배정되는 NIC 부분)에 있다. 팀 보유 대수 규모에서
 * 3바이트(최대 1600만 조합)면 충돌 걱정 없이 충분하고, RF 패킷 페이로드가
 * 빠듯한 상황(fhss_sync_packet 13바이트 와이어 포맷 등)이라 6바이트 전체보다
 * 가볍다. */
#define DEVICE_ID_LEN 3
